<section class="container  mt-5">
    <div class="row">

        {{#each data}}
        {{#if foto_c}}
        {{#if estado_curso}}

        <div class="col-md-7 div-curso mt-5  bg-cate mb-5 ">
            <figure>
                <div class="row">
                    <div class="col-md-6">
                        <img src="{{foto_c}}" class="img-fluid foto-cursos" alt="">
                    </div>
                    <div class="col-md-6">
                        <h3 class=" text-start p-1 mt-3">{{nombre_c}}</h3>
                        <h5 class=" text-start text-muted p-1 mb-4">{{nombre}}</h5>

                        <p class="text-start p-1">{{descripcion_c}}</p>
                    </div>
                </div>
                <div class="d-flex justify-content-end">
                    <a class="btn btn-warning text-white fw-bolder" href="/detalle-curso/{{id_c}}">Ver más </a>
                </div>
                {{!-- <a class="text-dark text-decoration-none" href="/detalles/{{id}}"> --}}



                    {{!--
                </a> --}}
            </figure>
        </div>
        {{/if}}
        {{/if}}
        {{/each}}
    </div>
</section>