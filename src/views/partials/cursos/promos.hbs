<section class="container m-auto mt-5">
    <div class="row">
        <h2 class="text-center mb-5">Promociones</h2>
        {{#each data}}
        {{#if foto_c}}
        {{#if estado_curso}}
        {{#if is_promo}}
        <div class="d-flex justify-content-center ">
            <div class="col-md-8 div-curso mb-5 bg-cate  ">


                <div class="row">
                    <div class="col-md-6 ">
                        <img src="{{foto_c}}" class="img-fluid foto-cursos " alt="">
                    </div>
                    <div class="col-md-6 ">
                        <div class="d-flex justify-content-end"> <span class="sale">Oferta</span></div>
                        <h3 class=" text-start mb-3 mt-3">{{nombre_c}}</h3>
                        <h5 class=" text-start text-muted ">{{nombre}}</h5>

                        <p class="text-start ">{{descripcion_c}}</p>

                    </div>

                </div>



                <div class="d-flex justify-content-end ">
                    <a class="btn btn-warning text-white fw-bolder" href="/detalle-curso/{{id_c}}">Ver más
                    </a>
                </div>
            </div>

        </div>
        {{/if}}
        {{/if}}
        {{/if}}
        {{/each}}
    </div>
</section>